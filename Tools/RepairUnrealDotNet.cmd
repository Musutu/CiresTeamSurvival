@echo off
echo Adds the .NET Framework 4.8 SDK and targeting pack needed by Unreal SwarmInterface.
echo Existing C++ build components are preserved. Windows will request administrator approval.
call "%~dp0SetupBuildTools.cmd"
