[/Script/LiveCoding.LiveCodingSettings]
; Agents build this project from many git worktrees at once; an editor with Live Coding active blocks every
; UnrealBuildTool build on the machine ("Unable to build while Live Coding is active"). Re-enable locally if wanted.
bEnabled=False
bPreloadProjectModules=False
